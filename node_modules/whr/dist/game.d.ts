import Player from './player';
import PlayerDay from './player-day';
export default class Game {
    day: number;
    whitePlayer: Player;
    blackPlayer: Player;
    winner: string;
    _handicap: number | Function;
    wpd: PlayerDay | null;
    bpd: PlayerDay | null;
    extras: object;
    constructor(black: Player, white: Player, winner: string, timeStep: number, handicap: number | Function, extras?: object);
    get inspect(): string;
    get handicap(): number;
    opponentsAdjustedGamma(player: Player): number;
    opponent(player: Player): Player;
    get predictionScore(): number;
    get whiteWinProbability(): number;
    get blackWinProbability(): number;
}
